import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Turbopack configuration removed to avoid internal errors
  // If issues persist, consider updating Next.js or using webpack
};

export default nextConfig;
