# Firebase Configuration
# Get these values from Firebase Console: https://console.firebase.google.com/
# Project Settings â†’ Your apps â†’ Web app (or create a new one)
NEXT_PUBLIC_FIREBASE_API_KEY=your-api-key-here
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your-sender-id
NEXT_PUBLIC_FIREBASE_APP_ID=your-app-id

# Admin API Secret (for server-side admin operations)
# Generate a strong random string for this
ADMIN_API_SECRET=your-strong-random-secret-here

# Optional: Firebase Service Account (for server-side admin operations)
# Only needed if using Firebase Admin SDK server-side
# FIREBASE_SERVICE_ACCOUNT={\
type\:\service_account\,...}
